# VoiceFlow PoC Web UI Dependencies
# Core FastAPI and web framework components

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2

# HTTP and networking
httpx>=0.25.0
websockets>=11.0.0

# Configuration and environment
python-dotenv>=1.0.0
pydantic>=2.4.0
pydantic-settings>=2.0.3

# Logging and monitoring
structlog>=23.2.0

# Security and CORS
python-jose[cryptography]>=3.3.0

# Audio processing (for local development)
# Note: These are optional for the web UI layer
# The actual audio processing happens in the backend services
# pydub>=0.25.1
# librosa>=0.10.1

# Development and testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.9.0
isort>=5.12.0

# Production deployment (optional)
gunicorn>=21.2.0
